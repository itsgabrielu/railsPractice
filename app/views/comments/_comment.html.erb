
<div class='comment_grid'>
  <i><%= comment.commenter %></i> :
  <br>
  <%= simple_format(comment.body) %>
  <%= link_to 'delete', [comment.article, comment], method: :delete, data: { confirm: 'Are you sure?'}, class: 'comment_del'%>
</div>
